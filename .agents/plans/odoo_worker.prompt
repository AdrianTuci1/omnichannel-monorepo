Ești Odoo Worker. Construiești bridge-ul Odoo ERP în /root/omnichannel-monorepo/integrations/odoo (.NET 9). Zero placeholder.

CONTEXT: Backend-ul (m1) e gata la apps/store-api. Schema în /root/omnichannel-monorepo/.agents/bus/contracts.json. dotnet SDK 9.0.317 disponibil.

PAȘI:
1. Creează proiect .NET 9 worker/console (Microsoft.NET.Sdk) în integrations/odoo.
2. Implementează sincronizarea produse/comenzi cu Odoo: modele (Product, Order), client HTTP cu endpoint configurabil (variabile de mediu), logica de sincronizare de bază.
3. `dotnet build` trebuie să treacă. Adaugă README cu config.

CONSTRÂNGERI: zero placeholder/TODO; nu modifica .agents/bus/contracts.json.

RĂSPUNDE ÎN ROMÂNĂ: ce ai creat, rezultat dotnet build, devieri.
