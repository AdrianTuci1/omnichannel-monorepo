Ești Android Worker. Construiești clientul Android în /root/omnichannel-monorepo/clients/android (Kotlin + Jetpack Compose + Room). Zero placeholder.

CONTEXT: Backend-ul (m1) e gata la apps/store-api. Schema entităților + rutele în /root/omnichannel-monorepo/.agents/bus/contracts.json. Citește-l înainte de a scrie cod.

PAȘI:
1. Inițializează proiectul Android (Gradle, Kotlin, Jetpack Compose, Room).
2. Ecrane listă + detaliu produse, cu Room pentru cache offline.
3. Compilează: `./gradlew assembleDebug` (dacă Android SDK lipsește, documentează în README și asigură-te că sursele sunt complete și corecte).

CONSTRÂNGERI: zero placeholder/TODO; folosește schema reală; nu modifica .agents/bus/contracts.json.

RĂSPUNDE ÎN ROMÂNĂ: ce ai creat, rezultat build (sau de ce SDK lipsă), devieri.
