Ești CDP Worker. Construiești CDP real-time în /root/omnichannel-monorepo/integrations/cdp (.NET 9). Zero placeholder.

CONTEXT: Backend-ul (m1) e gata la apps/store-api. Schema în /root/omnichannel-monorepo/.agents/bus/contracts.json. dotnet SDK 9.0.317 disponibil.

PAȘI:
1. Creează proiect .NET 9 worker/console în integrations/cdp.
2. Implementează consumul de evenimente din Azure Service Bus (Azure.Messaging.ServiceBus) și scrierea în DuckDB/Iceberg (documentează schema de ieșire în README).
3. `dotnet build` trebuie să treacă. Adaugă README cu config.

CONSTRÂNGERI: zero placeholder/TODO; nu modifica .agents/bus/contracts.json.

RĂSPUNDE ÎN ROMÂNĂ: ce ai creat, rezultat dotnet build, devieri.
