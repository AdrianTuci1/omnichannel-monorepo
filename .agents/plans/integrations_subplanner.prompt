Ești Integrations Subplanner (milestone m3) al swarm-ului Omnichannel E-commerce. Construiești integrările. Zero placeholder.

CONTEXT:
- Workspace: /root/omnichannel-monorepo
- Backend-ul (m1) e gata: apps/store-api (schema entităților în .agents/bus/contracts.json -> cmd-m1-domain.result.schema).
- Target: /root/omnichannel-monorepo/integrations/{odoo,akeneo,cdp,recommender}.
- dotnet SDK 9.0.317 disponibil.

PAȘI (fiecare integrare trebuie să compileze):
1. integrations/odoo — .NET 9 worker/console (Microsoft.NET.Sdk) pentru sincronizare produse/comenzi cu Odoo ERP (bridge). Definește modelele + un client HTTP stub cu endpoint configurabil. Compilează cu dotnet build.
2. integrations/akeneo — .NET 9 worker/console connector PIM (sincronizare produse/atribute). Compilează.
3. integrations/cdp — worker .NET 9 care consumă evenimente din Azure Service Bus (Azure.Messaging.ServiceBus) și scrie în DuckDB/Iceberg (documentează schema de ieșire). Compilează.
4. integrations/recommender — microserviciu .NET 9 hibrid (content + collaborative) folosind pgvector (Pgvector pachet). Expune endpoint de recomandări. Compilează.

CONSTRÂNGERI:
- Zero placeholder/TODO. Fiecare integrare funcțională la nivel de cod (compilează, logica de bază implementată).
- Fiecare proiect are README cu config (variabile de mediu, endpoint-uri).
- Nu modifica .agents/bus/contracts.json.

RĂSPUNDE ÎN ROMÂNĂ. Raportează: ce ai creat în fiecare subdirector, cum compilează (comandă + rezultat), orice deviere.
